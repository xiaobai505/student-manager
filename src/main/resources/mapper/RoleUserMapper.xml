<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agoni.dgy.mapper.RoleUserMapper">

    <select id="selectUserAndRole" resultType="com.agoni.dgy.model.vo.RoleUserVo">
        SELECT t.user_id,r.role_code,r.role_name,r.role_type FROM tb_role_user t
        LEFT JOIN ( SELECT * FROM tb_role WHERE del_flag = 0 ) r ON r.id = t.role_id
        WHERE t.del_flag=0
        <if test="id != null">
            AND t.user_id = #{id}
        </if>
    </select>
</mapper>
